-- Procedimiento para listar los numeros primos
CREATE OR REPLACE PROCEDURE lista_primos(N IN INT)
IS
NPRMOS INT:=0;
J INT:=1;
BEGIN

WHILE NPRIMOS<N LOOP
	IF ESPRIMO(J) THEN
		DBMS_OUTPUT.PUT_LINE(J);
		NPRIMOS:=NPRIMOS+1;
	END IF;
	J:=J+1;
END LOOP;
END;
/
