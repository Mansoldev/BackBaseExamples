SET SERVEROUTPUT ON
DECLARE
num INT:=0;
A INT:=5;
B INT:=5;
BEGIN
IF A > B THEN
	NUM:=A-B;
 	DBMS_OUTPUT.PUT_LINE(' LA DISTANCIA ES:'||NUM);
ELSE
	NUM:=B-A;
	DBMS_OUTPUT.PUT_LINE('LA DISTANCIA:'||NUM);
END IF;
END;
/
